// Standard library - collections::Vec
// Dynamic array

struct Vec {
    capacity: u64
}

protocol VecOps {
    function new() -> Vec;
    function len() -> u64;
    function is_empty() -> bool;
    function clear();
}
