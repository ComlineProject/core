// Standard library - http::Request

enum HttpMethod {
    GET
    POST
    PUT
    DELETE
}

struct Request {
    method: HttpMethod;
    uri: str;
}

protocol RequestOps {
    function new(method: HttpMethod, uri: str) -> Request;
}
