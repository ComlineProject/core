// Standard library - http::Response

struct Response {
    status_code: u16;
    body: str;
}

protocol ResponseOps {
    function new(status_code: u16) -> Response;
    function set_body(body: str);
}
